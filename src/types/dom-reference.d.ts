/// <reference lib="dom" />

// This file forces the DOM lib into TypeScript type-checking for environments
// (like some JSR/deno builds) that might not pick up `tsconfig.json` correctly.
// It is intentionally minimal and only used to provide DOM types during release.
